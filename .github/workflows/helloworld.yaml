name: helloworld
on:
  - push
jobs:
  greetings:
    runs-on: ubuntu-latest
    steps:
    - id: s00
      #running some shell bash command
      run: echo "current date time $(date)"
    - id: s01
      uses: mscoutermarsh/cowsays-action@master
      with:
        text: 'ship ship ship!'
        color: 'green'
 